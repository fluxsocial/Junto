{
    "name": "junto",
    "version": "0.0.1",
    "description": "A social network",
    "scripts": {
      "hc:build": "rm -rf tmp-storage && mkdir -p ./dna && cd junto && hc package",
      "hc:make-holoscape": "npm run hc:build && rm ui.zip && cd ui-src && npm install && npm run build && zip -r ../ui.zip ./*"
    },
    "author": "Joshua Parkin (josh@junto.foundation)",
    "license": "GPL-3"
  }